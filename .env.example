# Server-side only. Never expose this key with a NEXT_PUBLIC_ prefix.
OPENAI_API_KEY=

# Set an OpenAI Responses API model ID available to your API project.
# Kept configurable intentionally; the app does not hard-code a model.
OPENAI_MODEL=
